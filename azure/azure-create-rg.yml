---
- name: Create Azure resource group
  hosts: localhost
  gather_facts: false

  vars:
    location: "eastus"

  tasks:

    - name: Create Resource Group
      azure.azcollection.azure_rm_resourcegroup:
        name: "{{ res_group }}"
        location: "{{ location }}"
        state: present
